pipeline {
    agent any

    stages {
        stage('Hello Jenkins') {
            steps {
                echo 'Hey, This is my Jenkins pipeline.'
                sh 'echo "The code was successfully verified"'
            }
        }
    }
}
